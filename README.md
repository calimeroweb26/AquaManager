# AquaManager
Gestion des changements d eau pour aquariums.
Backend Node.js + SQLite - Frontend HTML - Nginx reverse proxy

Script installation auto lxc proxmox a executer dans le shell du proxmox

bash <(curl -s https://raw.githubusercontent.com/calimeroweb26/AquaManager/main/install.sh | sed 's/\r//')
